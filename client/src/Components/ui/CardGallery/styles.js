import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles((theme) => ({
  gallery: {
    position: 'relative',
    display: 'flex',
    flex: '0 1 auto',
    width: 'auto',
    height: 'auto',
    overflowX: 'auto',
  },
}))
