import { makeStyles } from 'tss-react/mui'

export const useStyles = makeStyles((theme) => ({
  gallery: {
    position: 'relative',
    display: 'flex',
    flex: '0 1 auto',
    width: 'auto',
    height: 'auto',
    overflowX: 'auto',
  },
}))
